http {
    # Настройка upstream для балансировки нагрузки
    upstream backend_servers {
        server backend1.example.com;
        server backend2.example.com;
        server backend3.example.com;
    }

    # Определяем зону для ограничения запросов (10 запросов в минуту)
    limit_req_zone $binary_remote_addr zone=partner_limit:10m rate=10r/m;

    server {
        listen 80;

        # Применяем ограничение для всех запросов
        location / {
            limit_req zone=partner_limit burst=5 nodelay;
            limit_req_status 429;  # Возвращаем 429 при превышении
            proxy_pass http://backend_servers;
        }
    }
}